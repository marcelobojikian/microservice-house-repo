spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:cashier-test
    username: sa
    password: 
    initialization-mode: never
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
